<select
  class="select {{ cssMix }}"
  name="{{ name }}"
  {{ 'data-submit' if submit }}
>
  {% for option in options %}
    <option value="{{ option.value }}">
      {{ option.title or option.value }}
    </option>
  {% endfor %}
</select>
